# The URL the site will be built for
base_url = "https://rolling.bux.fr"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# The site theme to use.
theme = "juice"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
juice_logo_name = "Rolling engine"
juice_logo_path = "RollingLogoSeul.png"
juice_extra_menu = [
    { title = "Code source serveur", link = "https://github.com/buxx/rolling"},
    { title = "Code source GUI", link = "https://github.com/buxx/rollgui"}
]
repository_url = "https://github.com/buxx/rolling"